import React from 'react';
import { ActivityIndicator } from 'react-native';

const LoadingIcon = () => {
  return <ActivityIndicator size="large" color={'#ed1b23ff'} />;
};

export default LoadingIcon;
